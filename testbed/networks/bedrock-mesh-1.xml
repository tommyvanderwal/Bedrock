<network>
  <name>bedrock-mesh-1</name>
  <bridge name='br-bedmesh1' stp='off' delay='0' macTableManager='kernel'/>
  <!-- multicast_snooping disabled at runtime via spawn.py prereq;
       libvirt's bridge XML schema doesn't expose the knob directly. -->
</network>
