<network>
  <name>bedrock-mesh-1</name>
  <!-- Isolated L2 mesh plane #1. All sims plug into this; every pair
       shares a path through it. Yankable per-bridge for chaos tests
       (virsh net-destroy / net-start). No NAT, no DHCP — peer
       discovery + APIPA-or-cluster-scheme assigns IPs at runtime. -->
  <bridge name='br-bedmesh1' stp='off' delay='0'/>
</network>
