{
  "summary": "Deep multi-agent study of DRBD 9 generation-UUID + quorum behavior to fix a frozen minority Primary rotating its UUID",
  "agentCount": 20,
  "logs": [],
  "result": {
    "final": {
      "rotation_explanation": "A current-UUID rotation in DRBD 9.3.2 is TWO-STAGE, and the two stages have DIFFERENT gates -- that asymmetry is the entire (A)/(B) split.\n\nSTAGE 1 -- ARM (synchronous, inside finish_state_change, drbd_state.c). When a Primary loses contact with a peer's data, lost_contact_to_peer_data() is true (drbd_state.c:3744 -- peer disk goes from a data-bearing state to D_UNKNOWN/D_OUTDATED/<D_INCONSISTENT, an ordinary disconnect). With role==Primary, !UNREGISTERED, and drbd_data_accessible(device,NEW) true (the local disk is still D_UP_TO_DATE -- this is NOT a quorum check; drbd_data_accessible consults only local/peer D_UP_TO_DATE, never have_quorum), create_new_uuid=true (drbd_state.c:3096-3099). The gate at 3135-3136: `if (create_new_uuid && !susp_uuid[OLD]) set_bit(__NEW_CUR_UUID, &device->flags)`. This only ARMS a pending flag; it does NOT touch the on-disk UUID. PRIMARY_LOST_QUORUM is latched in the SAME state change at 2885-2886, and susp_quorum at 2473-2474. The flag is armed in BOTH (A) and (B).\n\nSTAGE 2 -- EXECUTE (mint get_random_u64, stamp old current into the lost peer's bitmap slot via rotate_current_into_bitmap, drbd_md_sync). The actual on-disk write is reached by several routes. CORRECTION FROM VERIFICATION: the two immediate-bump conditions are NOT in finish_state_change and do NOT run synchronously. They live in w_after_state_change (drbd_state.c, function at ~3940), which is queued via queue_after_state_change_work -> drbd_queue_work onto resource->work and executed ASYNCHRONOUSLY by the dedicated resource->worker kernel thread (drbd_worker). The execution model is queued-async, not in-line. The routes:\n  - WRITE/MAKE_NEW_CUR_UUID route -> drbd_sender.c:3443 `if (device->have_quorum[NOW] && drbd_data_accessible(...))`. Quorum-gated; only runs after IO is admitted. A frozen zero-write primary never reaches it. SAFE.\n  - DISCONNECT route -> peer_device_disconnected() (called by conn_disconnect), drbd_receiver.c:9865 `if (!drbd_suspended(device))` AND 9886 `!test_bit(PRIMARY_LOST_QUORUM, &device->flags)`, with the explicit comment 'when we lost quorum ... do not create the new UUID immediately!'. Doubly blocked for a frozen quorum-lost primary. SAFE.\n  - IMMEDIATE post-state-change routes in w_after_state_change: drbd_state.c:4295-4305 (peer disk D_UP_TO_DATE -> D_FAILED/D_INCONSISTENT -> test_and_clear NEW_CUR_UUID -> new_current_uuid=true) and 4466-4471 (susp_uuid false->true edge -> test_and_clear NEW_CUR_UUID -> new_current_uuid=true; then `if (new_current_uuid) drbd_uuid_new_current(device, false)`). drbd_uuid_new_current itself (drbd_main.c:5273) carries NO quorum/suspend guard, and these two call sites add none. THE LEAK -- but it fires in worker context after the state change is applied, not synchronously.\n\nWHY (A) -- lose all 3 at once -- did NOT rotate: all peers go cleanly D_UP_TO_DATE -> D_UNKNOWN in one cascade; quorum drops in the same state change, PRIMARY_LOST_QUORUM latches, IO freezes. The immediate paths require a peer disk reaching D_FAILED/D_INCONSISTENT (a real write-failure transition, 4295) or a susp_uuid false->true bookkeeping edge (4466) -- a clean ->D_UNKNOWN partition produces neither. Nothing executes; current-UUID stays frozen at the pre-partition value. Correct, designed behavior.\n\nWHY (B) -- lose 2 of 3, keep 1 -- DID rotate despite zero writes: the surviving link drives a coordinated multi-node (2PC) state change as the peers leave one at a time. During that sequence the device traverses one of the two UNGUARDED w_after_state_change paths -- the susp_uuid suspend-bookkeeping edge at 4466 or the peer-disk D_UP_TO_DATE->D_FAILED/D_INCONSISTENT edge at 4295. Because these bypass the PRIMARY_LOST_QUORUM/have_quorum gate that protects the write and disconnect routes, the on-disk current-UUID rotates even though the resource is suspended:quorum and nothing was written. (Which of the two fires is a citation/checkout-pinning detail, not a runtime mystery; both are statically reachable. The 'new current UUID: <hex> weak: <hex>' dmesg line from drbd_main.c correlated with the preceding state-change line pins it exactly.)",
      "can_prevent_rotation": "PARTIALLY, and not purely from drbd.conf. The rotation in (B) is a KERNEL-SIDE escape of DRBD's own stated invariant ('when we lost quorum ... do not create the new UUID immediately!', drbd_receiver.c:9868). The two immediate-bump paths in w_after_state_change (drbd_state.c:4295-4305 and 4466-4471) lack the !PRIMARY_LOST_QUORUM/have_quorum gate that the write (drbd_sender.c:3443) and disconnect (drbd_receiver.c:9886) routes carry. No drbdsetup/drbd.conf option exists that says 'a frozen quorum-lost primary must not rotate its current-UUID' -- it is meant to be automatic, and IS for the all-peers-lost case (A). So you cannot tune the rotation away from userspace; the durable fix is a small kernel patch adding the quorum guard to those two paths in w_after_state_change (report to LINBIT with the A-vs-B reproducer + show-gi UUID dumps + the dmesg 'new current UUID' line, against 9.3.2 / utils 9.34.0).\n\nWhat you CAN do operationally / architecturally:\n(1) Make quorum loss ATOMIC with peer loss so the primary behaves like (A): shrink the denominator so the FIRST relevant disconnect already drops below quorum before any per-peer/2PC transition runs the immediate path. In practice use a diskless arbiter/witness to set the effective threshold, NOT quorum=all on 4 diskful nodes (which leaves a quorate window on the first loss).\n(2) Force-demote the LOSING side to Secondary (on-suspended-primary-outdated=force-secondary, which Bedrock already runs) BEFORE it ever reconnects, so it reintegrates as SyncTarget. NOTE confirmed by verification: this fires at RECONNECT and CANNOT precede the rotation -- the rotation is created in the same atomic state change that sets cached_susp, and force-secondary requires cached_susp as a precondition. So it cures recovery ergonomics (who yields), not the rotation itself.\n(3) Never run `drbdsetup new-current-uuid` on both sides of a survivor/secondary pair -- that manufactures exactly the divergence.\n\nThe only TRUE prevention of an incremental (non-full) resync is keeping the frozen side's current-UUID frozen, which today requires the kernel patch OR forcing the (A)-like atomic-quorum-loss path via a diskless witness.",
      "recommended_drbd_config": "Keep what you have, with a diskless witness providing the odd vote so a partition is genuinely quorate or genuinely sub-quorum (never the dangerous 'quorate-window-then-frozen' state quorum=all gives on 4 diskful nodes):\n\nresource <res> {\n  options {\n    quorum majority;                              # threshold = voters/2+1 (calc_quorum_at QOU_MAJORITY, drbd_state.c:1367). On 4 diskful + 1 diskless witness = 3-of-5. The 3-node survivor partition KEEPS quorum and stays Primary; the minority loses it cleanly. Avoids quorum=all's behavior (QOU_ALL -> voters, :1370) where losing ANY one peer immediately drops quorum and opens a quorate-then-frozen UUID-bump window.\n    on-no-quorum suspend-io;                       # LINBIT-recommended; freezes IO (susp_quorum), protects data vs io-error when apps don't exit cleanly on EIO. KEEP.\n    on-suspended-primary-outdated force-secondary; # KEEP. Frozen ex-primary that reconnects to a newer-generation quorate partition demotes to Secondary + fails pending/new IO -> clean SyncTarget. Requires on-no-quorum=suspend-io. Acts at RECONNECT (does NOT itself prevent the rotation -- verification confirms it provably fires AFTER it).\n    # quorum-minimum-redundancy: leave OFF -- only raises the bar to GAIN quorum; does not break even splits, does not stop UUID rotation.\n  }\n}\n\nALSO add a dedicated diskless tiebreaker/arbitrator node (DRBD's native equivalent of your external witness; User Guide 2.23.1) so the 4-diskful even cluster becomes odd-voter. That diskless node is the ONLY internal even-split breaker DRBD has (calc_quorum diskless tiebreaker, drbd_state.c:1571-1583), and verification confirms it CANNOT fire with zero diskless nodes: the gate diskless_majority_at = calc_quorum_at(QOU_MAJORITY, 0) = 1, and qd.diskless(0) >= 1 is false. With quorum=all and zero diskless nodes a 4-node cluster cannot break a 2/4 split internally -- hence the witness is mandatory.\n\nNOTE: quorum=all -> quorum=majority does NOT by itself guarantee the (B) rotation disappears on the MAJORITY side -- but verification UPGRADES the confidence here: under majority, a second primary is blocked by SS_TWO_PRIMARIES (drbd_state.c:1644-1646) and the minority cannot leak a committed write (io-error path -EIO drbd_req.c:696-700; suspend-io RQ_POSTPONED drbd_req.c:2057). So majority closes the committed-WRITE leak provably; the residual is only the no-write UUID rotation on the surviving link, which the kernel patch closes deterministically. The config change reduces blast radius; the patch closes it.",
      "bedrock_d_operations": "For bedrock-d's failover/heal path, the operations that matter:\n\n1. SINGLE-WRITER ON PROMOTE: only ONE partition may force-promote. Your netd election + witness DRBD-UUID claim is already sole writer of mgmt_master -- keep it. Two independent `drbdadm primary --force` from the same ancestor is the manufactured-divergence case. The survivor's force-promote legitimately rotates ITS current-UUID (Secondary->Primary, drbd_state.c:3131-3132) and stamps the lost peer's bitmap slot (rotate_current_into_bitmap, drbd_main.c:4837) -- correct and expected.\n\n2. DEMOTE THE LOSER BEFORE RECONNECT: drive the minority/frozen ex-primary to Secondary via on-suspended-primary-outdated=force-secondary (maybe_force_secondary, drbd_receiver.c:8089) so it reintegrates as SyncTarget. Bedrock should see the 'force secondary!' warn line on heal rather than after-sb StandAlone. If you see StandAlone, the (B) rotation already happened -- and verification confirms force-secondary CANNOT have prevented it (it requires cached_susp, which the rotation's own state change set).\n\n3. DO NOT mint UUIDs out-of-band: bedrock-d / any fence/quorum-lost handler must NEVER call `drbdsetup new-current-uuid` (forced path drbd_nl.c) on the frozen node -- indistinguishable from the leak. Audit your handler scripts for this.\n\n4. ATOMIC QUORUM-LOSS SIGNAL (the key architectural lever, per your arbiter/.254-owner design): make the witness/arbiter shrink the election denominator so the minority registers quorum loss in ONE step (mirroring (A)), instead of a staggered per-peer teardown that opens the 2PC window for the immediate-bump leak. Aligns with 'realtime is authority, rqlite follows' and your fast fence/demote posture.\n\n5. DRBD NOT up-on-boot for returning master: consistent with lesson_master_return_steals_back -- do not `drbdadm up` on a returning node before the witness/election has settled mastership; let it come back as SyncTarget.\n\n6. RECOVERY when split-brain DID occur (safety net): if heal lands in StandAlone/after-sb, run `drbdadm show-gi` on both sides first, then `drbdadm secondary --force` + `drbdadm connect --discard-my-data <res>` on the KNOWN-LOSER (the frozen ex-primary that wrote nothing) -- NEVER on the survivor.",
      "heal_mechanism": "On reconnect, drbd_uuid_compare() (drbd_receiver.c, function at 4678) decides no-sync vs incremental vs full vs split-brain by walking, in order: (1) current-UUIDs equal (self==peer branch opening at 4742) -> happy path, incl. RULE_LOST_QUORUM at 4785-4798 which returns SYNC_TARGET/SOURCE_IF_BOTH_FAILED (clean incremental) -- the intended quorum-failover resolution, living ONLY inside the self==peer branch; (2) my-current == peer's-bitmap-slot-for-me -> RULE_BITMAP_PEER -> SYNC_TARGET_USE_BITMAP (incremental); (3) my-bitmap == peer's-current -> RULE_BITMAP_SELF -> SYNC_SOURCE_USE_BITMAP (incremental); (4) current found only in the other's HISTORY -> *_SET_BITMAP (FULL resync); (5) once currents diverged (self==peer failed AND one-sided history checks 4879-4886 failed): both bitmap_uuids equal+nonzero -> RULE_BITMAP_BOTH -> SPLIT_BRAIN_AUTO_RECOVER (return 4892); shared history ancestor -> RULE_HISTORY_BOTH -> SPLIT_BRAIN_DISCONNECT (return 4907). Both carry is_split_brain; DISCONNECT forces need_full_sync_after_split_brain -> StandAlone + after-sb + --discard-my-data. (Line numbers and branch nesting CONFIRMED.)\n\nIf (B) had NOT rotated: the frozen primary's current-UUID still equals what the survivor recorded in peer_md[primary].bitmap_uuid at force-promote (rotate_current_into_bitmap stamped the old common-ancestor current there). Heal hits step (1) RULE_LOST_QUORUM (self==peer, frozen side advertises UUID_FLAG_PRIMARY_LOST_QUORUM -> forced SYNC_TARGET) or step (2) RULE_BITMAP_PEER -> incremental. Clean, fast, no operator.\n\nBecause (B) DID rotate: both sides hold sibling current-UUIDs off the same ancestor; self != peer; neither current is in the other's history; histories share the common-ancestor UUID -> step (5) RULE_HISTORY_BOTH -> SPLIT_BRAIN_DISCONNECT. Exactly the StandAlone/after-sb/--discard-my-data observed.\n\nKeeping the frozen zero-write primary's current-UUID frozen is precisely what keeps the survivor's bitmap slot a valid delta and forces step (1)/(2) incremental instead of step (5) split-brain. force-secondary does NOT repair already-diverged UUIDs (no uuid/bitmap ops in maybe_force_secondary, drbd_receiver.c:8089-8103, verified) -- it only changes WHO yields, after the fact.",
      "residual_risks": "1. Root-cause path identity: WHICH immediate-bump condition fires in (B) -- drbd_state.c:4466 susp_uuid-edge vs 4295 peer-disk-D_FAILED edge -- is a checkout-pinning/citation detail, not a runtime unknown. Both are statically reachable and unguarded in your 9.3.2 tree. Pin it exactly with the 'new current UUID: <hex> weak: <hex>' dmesg line (drbd_main.c) correlated with the preceding state-change line, or ftrace/bpftrace on rotate_current_into_bitmap / drbd_uuid_new_current on the live sims. Small residual that the rotation comes from a connect/disconnect handshake race rather than these two, but the disconnect route is doubly guarded, so it is unlikely.\n\n2. Execution-model correction: the immediate bump runs ASYNCHRONOUSLY in the resource->worker thread (w_after_state_change, queued via drbd_queue_work), NOT synchronously in finish_state_change. This does not change the conclusion (an unguarded path mints the UUID) but means any patch must add the !PRIMARY_LOST_QUORUM guard at the w_after_state_change call sites, and any race reasoning must account for worker-thread ordering relative to the suspend.\n\n3. quorum=majority is necessary, and STRONGER than first thought but still not a total guarantee for the no-write rotation: it provably blocks a committed-write leak (SS_TWO_PRIMARIES + io-error/RQ_POSTPONED) and removes the minority's quorate window. It does NOT provably stop the surviving-link 2PC on the MAJORITY side from hitting 4466/4295 with a zero-write rotation. The only deterministic fix for the rotation is the kernel patch.\n\n4. force-secondary timing: confirmed to fire at RECONNECT, AFTER both sides minted UUIDs (it requires cached_susp, set by the rotation's own state change). Safety net for recovery ergonomics, not prevention; resync may still be FULL (SET_BITMAP) depending on whether after-sb can relate the generations.\n\n5. The 9.2.18/9.3.2 ChangeLog fix ('diskless primary creates a new current UUID') targets the DISKLESS-primary path; it almost certainly does NOT cover your DISKFUL partial-partition case. Do not assume you are patched.\n\n6. Witness/arbiter interaction: an Outdated-marked missing node lets a partition 'always have quorum' (manpage); the QOU_ALL outdated-removal optimization (drbd_state.c:1548-1554) means a cleanly-Outdated peer keeps survivors quorate. Helps GRACEFUL leaves only -- an ABRUPT partition (your test) does not outdate peers.\n\n7. Cannot tune away from userspace: there is no drbd.conf knob for 'frozen primary must not rotate'. Mitigations reduce probability/blast-radius; only the kernel patch is deterministic.",
      "load_bearing_claims": [
        "CONFIRMED (drbdsrc_clone REL_VERSION 9.3.2, exact match to the Bedrock-pinned kmod-drbd9x-9.3.2 from build-iso.sh): drbd_state.c:3096-3099 sets create_new_uuid when a Primary loses peer-data contact (lost_contact_to_peer_data) and drbd_data_accessible(NEW) is true; drbd_data_accessible (6379-6401) is satisfied by local/peer D_UP_TO_DATE, with NO quorum reference -- quorum is the separate device->have_quorum field.",
        "CONFIRMED: drbd_state.c:3135-3136 `if (create_new_uuid && !susp_uuid[OLD]) set_bit(__NEW_CUR_UUID, &device->flags)` -- Stage 1 only ARMS an in-memory flag, no on-disk write; armed in both (A) and (B). (Synthesis cited 3134-3135; off-by-one, immaterial.)",
        "CONFIRMED: SAFE quorum-gated execution routes -- drbd_sender.c:3443 `if (device->have_quorum[NOW] && drbd_data_accessible(device, NOW))` gates drbd_uuid_new_current (the UUID-bump/MAKE_NEW_CUR_UUID route, not a literal data write); drbd_receiver.c:9865 `!drbd_suspended(device)` AND 9886 `!test_bit(PRIMARY_LOST_QUORUM,...)` in peer_device_disconnected (called by conn_disconnect), with the verbatim comment about not creating the UUID immediately on quorum loss.",
        "PARTLY REFUTED -> CORRECTED: the leak conditions drbd_state.c:4295-4305 (peer D_UP_TO_DATE->D_FAILED/D_INCONSISTENT) and 4466-4471 (susp_uuid false->true edge -> drbd_uuid_new_current(device,false)) are REAL and carry NO PRIMARY_LOST_QUORUM/suspend guard -- BUT they live in w_after_state_change (~3940), NOT finish_state_change, and run ASYNCHRONOUSLY in the resource->worker thread (queued via drbd_queue_work), not synchronously. Conclusion (unguarded path mints the UUID) stands; mechanism/location corrected.",
        "CONFIRMED: PRIMARY_LOST_QUORUM set at drbd_state.c:2885-2886 `if (role[NEW]==R_PRIMARY && !have_quorum[NEW]) set_bit(PRIMARY_LOST_QUORUM,...)`; susp_quorum set at 2473-2474 when on_no_quorum==ONQ_SUSPEND_IO.",
        "CONFIRMED: heal verdicts -- drbd_receiver.c:4785-4798 RULE_LOST_QUORUM returns SYNC_*_IF_BOTH_FAILED (clean incremental) ONLY inside the self==peer branch (opens 4742); 4888/return 4892 RULE_BITMAP_BOTH -> SPLIT_BRAIN_AUTO_RECOVER and 4894-4909/return 4907 RULE_HISTORY_BOTH -> SPLIT_BRAIN_DISCONNECT once currents diverged from a shared ancestor.",
        "CONFIRMED: hard mint gate drbd_main.c:4953-4966 -- __new_current_uuid_prepare calls rotate_current_into_bitmap, `if (!got_new_bitmap_uuid && !day0) return false;`, then `val = get_random_u64(); __drbd_uuid_set_current(device, val)` -- rotation is intrinsically tied to stamping a lost peer's bitmap slot (got_new_bitmap_uuid set only when a degraded/absent peer's slot is stamped).",
        "CONFIRMED: calc_quorum_at drbd_state.c:1367 QOU_MAJORITY -> voters/2+1, 1370 QOU_ALL -> voters; the diskless even-split tiebreaker (1571-1583) cannot fire with zero diskless nodes (diskless_majority_at = calc_quorum_at(QOU_MAJORITY,0)=1; qd.diskless(0)>=1 false). Caveat: a cleanly D_OUTDATED peer is removed from the denominator (1548-1554) so survivors can keep quorum even under quorum=all -- affects only the 'any single loss' wording.",
        "CONFIRMED: maybe_force_secondary drbd_receiver.c:8089-8103 acts only when !fail_io[NOW] && cached_susp && on_susp_primary_outdated==SPO_FORCE_SECONDARY, does a CS_HARD state change role[NEW]=R_SECONDARY on the reconnect/handshake path, with NO uuid/bitmap repair -- yields role only.",
        "VERIFICATION UPGRADE (synthesis claimed 'unverifiable from source'): (b) quorum=majority provably blocks a committed-write leak on the majority side -- second primary blocked by SS_TWO_PRIMARIES (drbd_state.c:1644-1646), minority writes fail -EIO (drbd_req.c:696-700) or held RQ_POSTPONED (drbd_req.c:2057); (c) force-secondary provably fires AFTER the rotation -- the rotation is created in the same atomic state change that sets cached_susp, which force-secondary requires as precondition. Only WHICH of 4295 vs 4466 fires remains a citation/checkout-pinning matter, resolvable from the dmesg 'new current UUID' line."
      ],
      "confidence": "high"
    },
    "verdicts": [
      {
        "claim": "In DRBD 9.3.2 (drbd_state.c:3096-3099), when a Primary loses peer-data contact, create_new_uuid is set if role==R_PRIMARY, the device is not UNREGISTERED, and drbd_data_accessible(NEW) is true; drbd_data_accessible is satisfied by local D_UP_TO_DATE alone, NOT by quorum. The cloned source at /home/tommy/projects/drbdsrc_clone/drbd is an exact-version match (REL_VERSION 9.3.2) to the module Bedrock nodes run.",
        "verdict": "CONFIRMED",
        "evidence": "VERSION: /home/tommy/projects/drbdsrc_clone/drbd/linux/drbd_config.h:14 says #define REL_VERSION \"9.3.2\". ChangeLog top entry is \"9.3.2 (api:genl2/proto:86-101,118-124/transport:21)\" matching the PRO_VERSION constants. Bedrock pins this exact build: /home/tommy/projects/Bedrock/installer/iso-build/build-iso.sh:206 fetches kmod-drbd9x-9.3.2-1.el${EL}_1.elrepo.x86_64.rpm. (The 8.4.11 drbd.ko reported by modinfo on THIS devbox is the in-tree kernel module on the Echo-stub workstation, not a Bedrock node — irrelevant, and DRBD is not even loaded here per empty lsmod.)\n\nEXACT LINES (drbd_state.c, inside finish_state_change(), defined at 2817):\n3096  if (lost_contact_to_peer_data(peer_disk_state)) {\n3097      if (role[NEW] == R_PRIMARY && !test_bit(UNREGISTERED, &device->flags) &&\n3098          drbd_data_accessible(device, NEW))\n3099          create_new_uuid = true;\nThis matches the claim verbatim, including the function name lost_contact_to_peer_data (\"loses peer-data contact\").\n\nlost_contact_to_peer_data (drbd_state.c:3744-3751) returns true exactly when peer disk went from real data (os >= D_INCONSISTENT && != D_UNKNOWN && != D_OUTDATED) to no-data-contact (ns < D_INCONSISTENT || ns == D_UNKNOWN || ns == D_OUTDATED) — i.e. a Primary losing peer-data contact, as claimed.\n\ndrbd_data_accessible (drbd_state.c:6379-6401), the load-bearing function:\n  if (device->disk_state[which] == D_UP_TO_DATE)\n      return true;\n  rcu_read_lock();\n  for_each_peer_device_rcu(peer_device, device) { ... if (peer_device->disk_state[which] == D_UP_TO_DATE) { data_accessible = true; break; } }\nIts doc comment (6375): \"returns true if either the local disk is up-to-date or of the peers.\" There is NO reference to quorum, have_quorum, or any vote count anywhere in the function. This confirms the core assertion: local D_UP_TO_DATE alone makes it true; quorum is not consulted.\n\nQUORUM IS A SEPARATE FIELD/PATH (refutation attempt failed): have_quorum is an independent bool[2] on the device (drbd_int.h:1639 \"bool have_quorum[2]; /* no quorum -> suspend IO or error IO */\"), not part of disk_state. The ONLY place quorum gates a UUID action is the LATER write step in drbd_sender.c:3443 (drbd_check_peers_new_current_uuid): \"if (device->have_quorum[NOW] && drbd_data_accessible(device, NOW)) drbd_uuid_new_current(...)\". That is a different code location (the actual disk write of the new UUID), not the flag-set at 3096-3099 the claim is about. Lines 3096-3099 only set create_new_uuid -> __NEW_CUR_UUID flag (3135-3136), with no quorum gate. So the claim's narrow scope (flag set on peer-data loss is quorum-independent and satisfied by local D_UP_TO_DATE) is exactly correct.\n\nCaveat (does not refute the claim): I could not directly observe the DRBD version running on the live Bedrock sims from this devbox; the \"exact match to running module\" rests on the installer pin (build-iso.sh:206) which installs precisely 9.3.2 from ELRepo, matching the cloned tree."
      },
      {
        "claim": "Verified drbd_state.c:3134-3135: `if (create_new_uuid && !susp_uuid[OLD]) set_bit(__NEW_CUR_UUID, &device->flags)` — Stage 1 only ARMS a flag; no on-disk UUID change. Armed in both (A) and (B).",
        "verdict": "CONFIRMED",
        "evidence": "Attempted to refute against LINBIT/drbd master HEAD a46cbd9 (REL_VERSION \"9.3.2\"), cloned to /tmp/drbdsrc. The substance holds on every point; only the line numbers are slightly off.\n\n1) EXACT CODE EXISTS. /tmp/drbdsrc/drbd/drbd_state.c, inside finish_state_change():\n  3134:  /* Only a single new current uuid when susp_uuid becomes true */\n  3135:  if (create_new_uuid && !susp_uuid[OLD])\n  3136:      set_bit(__NEW_CUR_UUID, &device->flags);\nSo the quoted statement maps to lines 3135-3136 (or 3134-3136 incl. the comment), NOT 3134-3135 as cited. The claim's \"3134-3135\" is a one-line-off, version-dependent citation error; line numbers shift across DRBD releases (shallow clone prevented full archaeology, but this is immaterial to the mechanism).\n\n2) STAGE 1 ONLY ARMS A FLAG — no on-disk write. The guarded body contains exactly one statement, set_bit(__NEW_CUR_UUID, &device->flags), an in-memory atomic bit op on device->flags. No drbd_md_sync, no drbd_uuid_new_current, no I/O in that branch (verified by reading lines 3126-3137 in full). Flag semantics confirm staging: drbd_int.h:579 __NEW_CUR_UUID = \"Set NEW_CUR_UUID as soon as state change visible\"; :578 NEW_CUR_UUID = \"Create new current UUID when thawing IO or issuing local IO\"; :580 WRITING_NEW_CUR_UUID; :592 MAKE_NEW_CUR_UUID = \"tell worker to ping peers and eventually write new current uuid\". Stage 2 promotion at drbd_state.c:902-908: after smp_wmb() (\"Make the NEW_CUR_UUID bit visible after the state change!\"), __NEW_CUR_UUID is cleared and NEW_CUR_UUID set. Actual on-disk write is downstream only: drbd_req.c:2263-2265 (NEW_CUR_UUID -> post MAKE_NEW_CUR_UUID work) -> drbd_sender.c:3447-3462 make_new_current_uuid() -> drbd_check_peers_new_current_uuid() -> drbd_uuid_new_current() (drbd_main.c:5273) -> __new_current_uuid_prepare + drbd_md_sync. None of that is reachable from Stage 1.\n\n3) \"ARMED IN BOTH (A) AND (B)\" — confirmed. create_new_uuid is a single bool declared once at drbd_state.c:2934 (init false), set true by 7 conditions (lines 3094,3099,3104,3109,3114,3129,3132), and consumed once at 3135. All triggers, including (A) disk-leaves-accessible-while-Primary (3127-3129: disk_state[OLD]>=D_INCONSISTENT && disk_state[NEW]<D_INCONSISTENT && role[NEW]==R_PRIMARY) and (B) promotion (3131-3132: role[OLD]==R_SECONDARY && role[NEW]==R_PRIMARY), converge on the same single Stage-1 set_bit. The !susp_uuid[OLD] guard (dedup) is corroborated by drbd_state.c:2476-2483 which sets susp_uuid[NEW]=true once a NEW_CUR_UUID is pending.\n\nNet: mechanism CONFIRMED. Only inaccuracy is the line citation (should be ~3135-3136, not 3134-3135) — cosmetic and release-dependent, does not affect correctness of the technical claim."
      },
      {
        "claim": "Verified SAFE quorum-gated execution routes in DRBD: drbd_sender.c:3443 `if (device->have_quorum[NOW] && drbd_data_accessible(device, NOW))` (write route); drbd_receiver.c:9865 & 9886, conn_disconnect path wrapped in BOTH `if (!drbd_suspended(device))` AND `!test_bit(PRIMARY_LOST_QUORUM, &device->flags)`, with the explicit comment 'when we lost quorum ... do not create the new UUID immediately!'",
        "verdict": "CONFIRMED",
        "evidence": "Tried to refute; the code citations hold against genuine upstream LINBIT/DRBD source. Cloned github.com/LINBIT/drbd, unshallowed for real history. HEAD = a46cbd9440cb9968b369b11e60338180b8a3912d, master, 2026-06-01 (Philipp Reisner). All cited lines exist (sender.c is 4040 lines, receiver.c is 11726 lines) and the text matches EXACTLY:\n\n1) /home/tommy/projects/drbdsrc/drbd/drbd_sender.c line 3443 is verbatim:\n   `if (device->have_quorum[NOW] && drbd_data_accessible(device, NOW))`\n   followed by `drbd_uuid_new_current(device, false);`. git blame attributes line 3443 to commit 60040bee01 (Philipp Reisner, 2020-05-27) — authentic.\n\n2) /home/tommy/projects/drbdsrc/drbd/drbd_receiver.c line 9865 is verbatim `if (!drbd_suspended(device)) {` and line 9886 is verbatim `!test_bit(PRIMARY_LOST_QUORUM, &device->flags) &&`. The full gating condition (lines 9884-9888) is: `if (!list_empty(&resource->transfer_log) && drbd_data_accessible(device, NOW) && !test_bit(PRIMARY_LOST_QUORUM, &device->flags) && test_and_clear_bit(NEW_CUR_UUID, &device->flags)) drbd_check_peers_new_current_uuid(device);`. The quoted comment exists verbatim (lines 9868-9872): \"We need to create the new UUID immediately when we finish requests that did not reach the lost peer. But when we lost quorum we are going to finish those requests with error, therefore do not create the new UUID immediately!\" The claim's elided quote 'when we lost quorum ... do not create the new UUID immediately!' is a faithful condensation. git blame: the comment and PRIMARY_LOST_QUORUM/drbd_suspended wrapper are commit 195e0a3574 (Philipp Reisner, 2017-11-22); the extra dispatch-guard paragraph is f792cad280 (Lars Ellenberg, 2026-04-02). All real DRBD maintainers — not injected.\n\nTWO IMPRECISIONS IN THE CLAIM (do not change the CONFIRMED verdict on line/text/comment accuracy, but worth flagging):\n(a) The receiver.c gate is literally inside `peer_device_disconnected()` (begins line 9853), NOT directly in `conn_disconnect()`. conn_disconnect() (line 9995) CALLS peer_device_disconnected() at line 10062, so the attribution is correct only via the call chain.\n(b) Both gates protect NEW-CURRENT-UUID CREATION, not a literal data-write submission path. sender.c:3443 is in drbd_check_peers_new_current_uuid(), reached via make_new_current_uuid() <- do_device_work() on the MAKE_NEW_CUR_UUID work bit, gating drbd_uuid_new_current(). receiver.c:9888 calls the same drbd_check_peers_new_current_uuid() on the disconnect path. Calling sender.c:3443 \"the write route\" is a mislabel — it is the UUID-bump route, which is quorum-gated precisely so a primary that lost quorum does NOT mint a new data generation. The claim's core safety assertion (these routes are quorum/data-accessibility gated) is correct; only the \"write route\" label and the conn_disconnect function name are loose."
      },
      {
        "claim": "THE LEAK, verified: drbd_state.c:4295-4305 (peer disk D_UP_TO_DATE->D_FAILED/D_INCONSISTENT -> test_and_clear NEW_CUR_UUID -> new_current_uuid=true) and drbd_state.c:4466-4471 (susp_uuid false->true edge -> test_and_clear NEW_CUR_UUID -> new_current_uuid=true; then `if (new_current_uuid) drbd_uuid_new_current(device, false)`) — BOTH call drbd_uuid_new_current SYNCHRONOUSLY inside finish_state_change with NO PRIMARY_LOST_QUORUM guard and NO suspend check.",
        "verdict": "REFUTED",
        "evidence": "Verified against LINBIT/drbd master (commit a46cbd9440cb, 2026-06-01), files /tmp/drbdsrc/drbd/drbd_state.c and /tmp/drbdsrc/drbd/drbd_main.c.\n\nThe code-mechanics half of the claim is ACCURATE, but the load-bearing structural assertion (\"SYNCHRONOUSLY inside finish_state_change\") is FALSE.\n\n(1) Line citations verified exact. drbd_state.c:4295-4305: `if (peer_disk_state[OLD] == D_UP_TO_DATE && (peer_disk_state[NEW] == D_FAILED || peer_disk_state[NEW] == D_INCONSISTENT) && test_and_clear_bit(NEW_CUR_UUID, &device->flags)) ... new_current_uuid = true;`. drbd_state.c:4466-4471: `if (!susp_uuid[OLD] && susp_uuid[NEW] && test_and_clear_bit(NEW_CUR_UUID, &device->flags)) new_current_uuid = true;` then `if (new_current_uuid) drbd_uuid_new_current(device, false);`. Neither site carries an inline PRIMARY_LOST_QUORUM or suspend check, and drbd_uuid_new_current (drbd_main.c:5273) has no quorum/suspend guard internally — that sub-part holds.\n\n(2) WRONG ENCLOSING FUNCTION. Both line ranges are inside w_after_state_change (definition at drbd_state.c:3940), NOT finish_state_change (definition at drbd_state.c:2817, body ends before line 3345). A grep for `drbd_uuid_new_current` and `new_current_uuid` across lines 2817-3344 (the entire finish_state_change body) returns ZERO matches. So the claim's headline location is factually incorrect.\n\n(3) NOT SYNCHRONOUS. In ___end_state_change (drbd_state.c:804): line 832 calls finish_state_change synchronously, but the after-state-change work is built separately at line 838 (alloc_after_state_change_work) and dispatched at line 930 via queue_after_state_change_work, which sets work->w.cb = w_after_state_change and calls drbd_queue_work(&resource->work, &work->w) (drbd_state.c:798). drbd_queue_work (drbd_main.c:3535-3543) only does `list_add_tail(&w->list, &q->q)` + `wake_up(&q->q_wait)` — it does NOT execute the callback. The resource->work queue is drained by resource->worker, a dedicated kernel thread running drbd_worker (registered at drbd_main.c:3789, started 3806). Therefore drbd_uuid_new_current runs ASYNCHRONOUSLY in worker-thread context after the state change is already applied, not synchronously within finish_state_change.\n\nNet: the UUID-bump conditions and the missing-guard observation are real, but the claim mislocates them (w_after_state_change, not finish_state_change) and mischaracterizes the execution model (queued/async worker, not synchronous). Because both the named function and the synchronous-execution assertion are false, the claim as stated is refuted."
      },
      {
        "claim": "PRIMARY_LOST_QUORUM is set at drbd/drbd_state.c:2885-2886 via `if (role[NEW]==R_PRIMARY && !have_quorum[NEW]) set_bit(PRIMARY_LOST_QUORUM,...)`; susp_quorum is set at lines 2473-2474 for on_no_quorum==ONQ_SUSPEND_IO.",
        "verdict": "CONFIRMED",
        "evidence": "Attempted to refute against LINBIT/drbd source (git clone --depth 1, HEAD a46cbd9 dated 2026-06-01, REL_VERSION \"9.3.2\"). Both line numbers and the code logic match verbatim — no refutation found.\n\n(1) PRIMARY_LOST_QUORUM, /tmp/drbdsrc/drbd/drbd_state.c lines 2885-2886, inside finish_state_change() (function starts at 2817):\n  2885:  if (role[NEW] == R_PRIMARY && !have_quorum[NEW])\n  2886:    set_bit(PRIMARY_LOST_QUORUM, &device->flags);\nThis is the exact two-line span the claim cites (2885-2886). `role` is `resource->role` (line 2819); `have_quorum` is the local `bool *have_quorum = device->have_quorum;` (line 2838), so `have_quorum[NEW]` == `device->have_quorum[NEW]`. The claim's transcription `role[NEW]==R_PRIMARY && !have_quorum[NEW]` is character-accurate. grep confirms this is the only set_bit(PRIMARY_LOST_QUORUM) site in the file (other hits at 3150-3152 build MDF metadata flag, 3231 clears it).\n\n(2) susp_quorum, /tmp/drbdsrc/drbd/drbd_state.c lines 2473-2474, inside sanitize_state() (function starts at 2051):\n  2473:  resource->susp_quorum[NEW] =\n  2474:    resource->res_opts.on_no_quorum == ONQ_SUSPEND_IO ? !resource_has_quorum : false;\nThis matches the claimed lines 2473-2474 and the condition on_no_quorum==ONQ_SUSPEND_IO. `resource_has_quorum` is a local bool (declared line 2059, set false at 2448 when any device lacks have_quorum[NEW]); when on_no_quorum != ONQ_SUSPEND_IO the ternary yields false. grep confirms ONQ_SUSPEND_IO appears only at 2474 and 3431 (the latter an early-return guard in state_change_is_susp_quorum), so 2473-2474 is the assignment site.\n\nBoth claimed locations and the surrounding logic are accurate; nothing contradicts the claim."
      },
      {
        "claim": "drbd_receiver.c:4785-4798 RULE_LOST_QUORUM returns SYNC_*_IF_BOTH_FAILED (clean incremental) ONLY inside the self==peer branch; drbd_receiver.c:4888 RULE_BITMAP_BOTH -> SPLIT_BRAIN_AUTO_RECOVER and 4894-4909 RULE_HISTORY_BOTH -> SPLIT_BRAIN_DISCONNECT when currents diverged from a shared ancestor.",
        "verdict": "CONFIRMED",
        "evidence": "Verified against LINBIT/drbd HEAD (commit a46cbd9, 2026-06-01) in /tmp/drbdsrc/drbd/drbd_receiver.c. All cited line numbers match exactly.\n\n(1) LINE NUMBERS / STRUCTURE — function is static enum sync_strategy drbd_uuid_compare() opening at line 4678-4680. A brace-depth trace (function body = depth 1) confirms:\n- `if (self == peer) {` opens at line 4742 -> depth 2. Here self=resolved current UUID (4720), peer=peer_device->current_uuid (4721).\n- RULE_LOST_QUORUM at line 4785 sits at depth 2 -> INSIDE the self==peer branch. Its four returns (lines 4788, 4792, 4796-4798) are all SYNC_TARGET_IF_BOTH_FAILED / SYNC_SOURCE_IF_BOTH_FAILED. No split-brain return exists in that block.\n- Line 4834 `}` drops back to depth 1, closing the self==peer branch.\n- RULE_BITMAP_BOTH (line 4888) and RULE_HISTORY_BOTH (line 4894) are at depth 1 -> OUTSIDE the self==peer branch (reached only after self==peer failed at 4742 AND the one-sided RULE_HISTORY_PEER/SELF checks at 4879-4886 failed, i.e. the two current UUIDs have diverged).\n\n(2) RETURN VALUES — line 4892 `return SPLIT_BRAIN_AUTO_RECOVER;` (guarded by self==peer && self!=0 on the two bitmap UUIDs); line 4907 `return SPLIT_BRAIN_DISCONNECT;` (returned when a self-history UUID matches a peer-history UUID, i.e. a shared/common ancestor — the loop comment at 4897-4899 explicitly calls it a \"common ancestor\"). Exactly as claimed.\n\n(3) SEMANTIC FRAMING CORROBORATED IN-SOURCE — comment at lines 5176-5177: \"RULE_LOST_QUORUM means that the current UUIDs are equal. The resync direction was found by looking if a node lost quorum while being primary.\" This independently confirms RULE_LOST_QUORUM only fires when current UUIDs are equal (self==peer). The enum sync_strategy table (line 120) and sync_descriptors[] (line 155) show SYNC_*_IF_BOTH_FAILED carry is_sync_source/is_sync_target (a normal bitmap-based incremental resync, \"clean incremental\"), whereas SPLIT_BRAIN_AUTO_RECOVER (207) and SPLIT_BRAIN_DISCONNECT (212) both carry is_split_brain=true. Downstream (lines 5232-5266) only SPLIT_BRAIN_DISCONNECT sets need_full_sync_after_split_brain, while SPLIT_BRAIN_AUTO_RECOVER is eligible for after-sb auto-resolution into an incremental direction — consistent with the AUTO_RECOVER vs DISCONNECT distinction in the claim.\n\nNo contradicting return path was found. Every factual assertion in the claim (line numbers, branch nesting, rule->strategy mappings, and the \"currents diverged from a shared ancestor\" condition) is borne out by the source."
      },
      {
        "claim": "Verified the hard mint gate drbd_main.c:4955-4965: __new_current_uuid_prepare calls rotate_current_into_bitmap first; `if (!got_new_bitmap_uuid && !day0) return false;` then `val = get_random_u64(); __drbd_uuid_set_current(device, val)` — rotation is intrinsically tied to stamping a lost peer's bitmap slot.",
        "verdict": "CONFIRMED",
        "evidence": "Cloned LINBIT/drbd at HEAD commit a46cbd9 (2026-06-01) and read /tmp/drbdsrc/drbd/drbd_main.c. The function __new_current_uuid_prepare (lines 4947-4977) verifies every material point:\n\nLINE-EXACT (4953-4966):\n  4953  spin_lock_irq(&device->ldev->md.uuid_lock);\n  4954  day0 = drbd_uuid_is_day0(device);\n  4955  got_new_bitmap_uuid = rotate_current_into_bitmap(device,\n  4956                  forced ? initial_resync_nodes(device) : 0,\n  4957                  device->resource->dagtag_sector);\n  4959  if (!got_new_bitmap_uuid && !day0) {\n  4960      spin_unlock_irq(&device->ldev->md.uuid_lock);\n  4961      return false;\n  4962  }\n  4964  old_current_uuid = device->ldev->md.current_uuid;\n  4965  val = get_random_u64();\n  4966  __drbd_uuid_set_current(device, val);\n\nThe two quoted code fragments are byte-exact against source: the gate `if (!got_new_bitmap_uuid && !day0)` -> `return false;` (4959-4962, claim collapsed the brace block to one line, semantically identical) and the mint `val = get_random_u64(); __drbd_uuid_set_current(device, val)` (4965-4966).\n\nSEMANTIC THESIS CONFIRMED. rotate_current_into_bitmap (4837-4910) sets got_new_bitmap_uuid |= NODE_MASK(node_id) only when it stamps prev_c_uuid (the old current UUID) into a peer's bitmap slot. First loop, the trigger at 4873-4878: `if ((pdsk <= D_UNKNOWN && pdsk != D_NEGOTIATING) || (NODE_MASK(node_id) & weak_nodes)) { peer_md[node_id].bitmap_uuid = prev_c_uuid; ... got_new_bitmap_uuid |= NODE_MASK(node_id); }`. Per the disk-state enum in drbd-headers/linux/drbd.h (248-260), `pdsk <= D_UNKNOWN && pdsk != D_NEGOTIATING` selects DISKLESS/ATTACHING/DETACHING/FAILED/INCONSISTENT/OUTDATED/UNKNOWN — i.e. lost/down/degraded peers (D_UNKNOWN is the peer state on a dropped connection). Second loop (4881-4906) does the same for fully-absent peers. So got_new_bitmap_uuid is non-zero precisely when at least one lost peer's bitmap slot was stamped, and the mint at 4965-4966 is gated on that. Minting without stamping is impossible EXCEPT the day0 case — exactly the exception the claim already carries via `&& !day0` (drbd_uuid_is_day0 documented at 4806-4835 as a fresh device that has never rotated).\n\nREFUTATION ATTEMPT FAILED. Two cosmetic nits, neither material: (1) __drbd_uuid_set_current is at line 4966, one line past the cited 4955-4965 upper bound (off-by-one in the range label only). (2) The claim says rotate_current_into_bitmap is called \"first,\" but drbd_uuid_is_day0 is actually called one line earlier (4954); rotate is the first METADATA-MUTATING step and precedes the mint, so \"first\" is defensible in context. All quoted text and the central gating thesis hold."
      },
      {
        "claim": "Verified calc_quorum_at: drbd_state.c:1366-1370 QOU_MAJORITY -> voters/2+1, QOU_ALL -> voters; with quorum=all on 4 diskful nodes ANY single peer loss drops quorum, and the diskless even-split tiebreaker (1571-1583) cannot fire with zero diskless nodes.",
        "verdict": "CONFIRMED",
        "evidence": "Verified against LINBIT/drbd HEAD a46cbd9 (2026-06-01). Cited line numbers match the current source exactly.\n\n(1) calc_quorum_at at /tmp/drbdsrc/drbd/drbd_state.c:1361-1377: `case QOU_MAJORITY: quorum_at = voters / 2 + 1;` (line 1367), `case QOU_ALL: quorum_at = voters;` (line 1370). QOU constants confirmed sentinels in drbd-headers/linux/drbd.h:444-446 (QOU_OFF=0, QOU_MAJORITY=1024, QOU_ALL=1025), so they hit the switch case labels, not the `default: quorum_at = setting` branch. CONFIRMED.\n\n(2) The diskless tiebreaker at drbd_state.c:1571-1583 is gated on `qd.diskless >= diskless_majority_at` where `diskless_majority_at = calc_quorum_at(QOU_MAJORITY, qd.diskless + qd.missing_diskless)` (line 1557). qd.diskless increments ONLY for an intentional-diskless peer (with-disk path line 1448: `if (disk_state == D_DISKLESS && is_intentional_diskless)`; no-disk path lines 1471-1472,1509-1510). With 4 diskful, non-intentional-diskless nodes, qd.diskless=0 and qd.missing_diskless=0, so diskless_majority_at = calc_quorum_at(QOU_MAJORITY, 0) = 0/2+1 = 1, and `0 >= 1` is false. Tiebreaker cannot fire. CONFIRMED. (Notably the other three tiebreaker clauses DO pass in this scenario: voters=4 so voters!=0 and voters%2==0 are true, and up_to_date+present == quorum_at-1 i.e. 3==3 is true; the diskless clause is the SOLE blocker — exactly as the claim implies.)\n\n(3) quorum=all, 4 diskful nodes, single peer abruptly lost (D_UNKNOWN): line 1552 voters = up_to_date+present+unknown = 3+0+1 = 4; quorum_at = calc_quorum_at(QOU_ALL,4) = 4; have_quorum = (up_to_date+present) >= quorum_at && ... = (3 >= 4) = false. Quorum dropped. CONFIRMED for the normal abrupt-failure case.\n\nCAVEAT (does not change verdict): \"ANY single peer loss drops quorum\" is not literally true for EVERY loss mode. If the lost peer becomes cleanly D_OUTDATED, the outdated-removal optimization at lines 1548-1554 sets voters = up_to_date+present = 3, quorum_at = calc_quorum_at(QOU_ALL,3) = 3, and (3 >= 3) = true, so survivors KEEP quorum. This affects only the wording of the parenthetical \"any single peer loss\"; it does not affect the load-bearing conclusion (the tiebreaker cannot fire with zero diskless nodes), which holds in all cases. DRBD 9 user guide (linbit.com 16.2/2.23) corroborates: the diskless tiebreaker requires \"adding a third, diskless node ... which will then act as a quorum tiebreaker.\""
      },
      {
        "claim": "maybe_force_secondary (drbd_receiver.c:8089) acts only when cached_susp && on_susp_primary_outdated==SPO_FORCE_SECONDARY, performs a hard state change role[NEW]=R_SECONDARY at RECONNECT, yielding the role without repairing UUIDs.",
        "verdict": "CONFIRMED",
        "evidence": "Source: LINBIT/drbd (git clone --depth 1, /tmp/drbdsrc).\n\nLOCATION exact: grep -> \"drbd/drbd_receiver.c:8089:static void maybe_force_secondary(struct drbd_peer_device *peer_device)\".\n\nFUNCTION BODY (drbd_receiver.c:8089-8103) verbatim:\n  static void maybe_force_secondary(struct drbd_peer_device *peer_device)\n  {\n      struct drbd_resource *resource = peer_device->device->resource;\n      unsigned long irq_flags;\n      if (!resource->fail_io[NOW] && resource->cached_susp &&\n          resource->res_opts.on_susp_primary_outdated == SPO_FORCE_SECONDARY) {\n          drbd_warn(peer_device, \"force secondary!\\n\");\n          begin_state_change(resource, &irq_flags,\n                     CS_VERBOSE | CS_HARD | CS_FS_IGN_OPENERS);\n          resource->role[NEW] = R_SECONDARY;\n          /* resource->fail_io[NEW] gets set via CS_FS_IGN_OPENERS */\n          end_state_change(resource, &irq_flags, \"peer-state\");\n      }\n  }\n\nCONDITION: claim's two cited predicates (cached_susp, on_susp_primary_outdated==SPO_FORCE_SECONDARY) are present (lines 8094-8095). NUANCE: the real guard ALSO leads with !resource->fail_io[NOW] (skip if IO already failing), which the claim omits but does not contradict.\n\nHARD STATE CHANGE: CS_HARD defined in drbd_state.h:31 = \"Forced state change, such as a connection loss\". role[NEW]=R_SECONDARY is the sole role mutation (line 8099).\n\nRECONNECT context: both callers are inside receive_state (peer-state handshake). Caller drbd_receiver.c:8116 (diskless_with_peers_different_current_uuids, \"Remote node has more recent data\") then set_bit(CONN_HANDSHAKE_RETRY). Caller 8341 sits in the `if (strategy_descriptor(strategy).reconnect)` retry-connect branch; reconnect=true strategies include SYNC_TARGET_PRIMARY_RECONNECT (descriptor table line 240-244).\n\nNO UUID REPAIR: awk over lines 8089-8103 found NO uuid/drbd_uuid/bitmap/resync operations in the body. Only effects are role demotion + fail_io (set via CS_FS_IGN_OPENERS, corroborated in drbd_state.c:1719 / fail_io[NEW] handling). Enum SPO_FORCE_SECONDARY defined in drbd-headers/linux/drbd.h:78-81 (SPO_DISCONNECT, SPO_FORCE_SECONDARY).\n\nExternal corroboration: drbd.conf-9.0 manpage (manpages.debian.org) states force-secondary \"will demote to secondary immediately, and fail all pending (and new) IO requests with IO errors\" - role yield + IO failure, no mention of data/UUID repair.\n\nFiles (absolute): /tmp/drbdsrc/drbd/drbd_receiver.c, /tmp/drbdsrc/drbd/drbd_state.h, /tmp/drbdsrc/drbd/drbd_state.c, /tmp/drbdsrc/drbd/drbd-headers/linux/drbd.h."
      },
      {
        "claim": "UNVERIFIABLE FROM SOURCE (needs live trace): (a) which of \"4295 vs 4466\" fires in scenario (B); (b) whether quorum=majority fully closes the split-brain write leak on the majority side; (c) whether force-secondary fires before \"the rotation\" in any ordering.",
        "verdict": "REFUTED",
        "evidence": "The claim asserts these three items need a live trace. Two of the three are in fact statically determinable from DRBD source (cloned LINBIT/drbd, submodule drbd-headers initialized), so the \"needs live trace\" framing is overstated.\n\n(b) quorum=majority leak closure — VERIFIABLE, determinate. calc_quorum_at() (drbd/drbd_state.c:1361): \"case QOU_MAJORITY: quorum_at = voters / 2 + 1;\" (QOU_MAJORITY=1024, drbd/drbd-headers/linux/drbd.h:445). Promotion is gated in __is_valid_soft_transition (drbd/drbd_state.c:1760-1775): a transition to R_PRIMARY without quorum returns SS_NO_QUORUM unless \"qi.up_to_date + qi.present >= qi.quorum_at\". The majority partition promotes precisely because it clears quorum_at; the minority/old primary cannot leak a *committed* write: under on-no-quorum=io-error the write completes with -EIO (drbd/drbd_req.c:696-700: \"quorum = ... cached_all_devices_have_quorum ...; m->error = ok && quorum ? 0 : (error ?: -EIO)\"); under suspend-io new writes are held as RQ_POSTPONED and never acknowledged (drbd/drbd_req.c:2057 \"if (drbd_suspended(device)) { req->local_rq_state |= RQ_POSTPONED; ...}\"). A second primary is independently blocked by SS_TWO_PRIMARIES (drbd/drbd_state.c:1644-1646). So \"fully closes the leak on the majority side\" is answerable from source, not a live-trace question.\n\n(c) force-secondary vs rotation ordering — VERIFIABLE, determinate. \"rotation\" = new-current-UUID rotation (rotate_current_into_bitmap, drbd/drbd_main.c:4837). The rotation is created inside the SAME atomic state change that produces the suspend: sanitize_state sets __NEW_CUR_UUID when \"create_new_uuid && !susp_uuid[OLD]\" on lost_contact_to_peer_data for a primary (drbd/drbd_state.c:3095-3136), and finish_state_change sets resource->cached_susp = resource_is_suspended(NEW) in the same transition (drbd/drbd_state.c:850). Force-secondary requires cached_susp to ALREADY be true: maybe_force_secondary (drbd/drbd_receiver.c:8089-8095) runs only \"if (!resource->fail_io[NOW] && resource->cached_susp && on_susp_primary_outdated == SPO_FORCE_SECONDARY)\" and only on the reconnect/handshake path (call sites 8116, 8341; also apply_local_state_change at 8042/connect_work, drbd/drbd_receiver.c:843,914). The drbd.conf-9.0 manpage states the precondition explicitly: force-secondary applies only after \"the primary already has suspended I/O due to lost quorum before detecting the outdated condition.\" Therefore the rotation necessarily precedes force-secondary in every ordering; force-secondary cannot fire before it. Determinate from source.\n\n(a) \"4295 vs 4466\" — genuinely not resolvable, but for a citation reason, not a runtime reason. These are line numbers into a different checkout than current HEAD: at HEAD they land on non-meaningful lines (drbd_receiver.c:4295 = \"break;\", :4466 = \"}\"; drbd_state.c:4295/4466 and drbd_nl.c:4295/4466 land elsewhere). They are NOT DRBD error codes (enum drbd_ret_code tops out at AFTER_LAST_ERR_CODE ~176, drbd/drbd-headers/linux/drbd.h:109-247). The two drbd_state.c regions near those lines both involve test_and_clear_bit(NEW_CUR_UUID, ...) in finish_state_change (drbd/drbd_state.c:4297,4304,4467) — i.e., statically reachable UUID-rotation cleanup whose entry conditions are visible in source. So even (a) is a source/static matter once the original checkout is pinned, not a live-trace matter.\n\nNet: the claim mislabels statically-answerable behavior as needing a live trace. Files examined: /tmp/drbdsrc/drbd/drbd_state.c, /tmp/drbdsrc/drbd/drbd_req.c, /tmp/drbdsrc/drbd/drbd_receiver.c, /tmp/drbdsrc/drbd/drbd_main.c, /tmp/drbdsrc/drbd/drbd-headers/linux/drbd.h. Corroborated by drbd.conf-9.0 manpage and DRBD 9 User Guide 2.23."
      }
    ]
  }
}